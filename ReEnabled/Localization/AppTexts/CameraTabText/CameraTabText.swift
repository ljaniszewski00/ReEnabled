enum CameraTabText {
    case mainRecognizerText(MainRecognizerText)
    case documentScannerText(DocumentScannerText)
    case colorDetectorText(ColorDetectorText)
    case lightDetectorText(LightDetectorText)
}
